<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Inventory Application: <%= title %></title>
</head>
<body>
    <%- include('../partials/header.ejs') %>
    <main>
        <form action="/games/<%= item.id %>" method="post">
            <label for="title">Title:</label>
            <input type="text" name="title" id="title" value="<%= item.title %>">
            <br>
            <label for="release_year">Release Year:</label>
            <input type="number" name="release_year" id="release_year" min="2000" value="<%= item.release_year %>">
            <br>
            <label for="cover">Cover:</label>
            <input type="url" name="cover" id="cover" value="<%= item.cover %>">
            <br>
            <button type="submit">Save</button>
        </form>
        <a href="/games/<%= item.id %>">Cancel</a>
    </main>
    <%- include('../partials/footer.ejs') %>
</body>
</html>