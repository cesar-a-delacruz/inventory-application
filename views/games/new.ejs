<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Inventory Application: <%= title %></title>
    <link rel="stylesheet" href="/new.css">
    <link rel="stylesheet" href="/partials/header.css">
    <link rel="stylesheet" href="/partials/footer.css">
</head>
<body>
    <%- include('../partials/header.ejs') %>
    <main>
        <form action="/games/new" method="post">
            <label for="title">Title:</label>
            <input type="text" name="title" id="title">
            
            <label for="release_year">Release Year:</label>
            <input type="number" name="release_year" id="release_year" min="2000">

            <label for="cover">Cover:</label>
            <input type="url" name="cover" id="cover">

            <label for="genres">Genre(s):</label>
            <select name="genres[]" id="genres" size="<%= genres.length %>" multiple>
                <% genres.map((genre) => { %>
                    <option value="<%= genre.id %>"><%= genre.title %></option>
                <% }) %>
                <option value=""></option>
            </select>

            <label for="developers">Developer(s):</label>
            <select name="developers[]" id="developers" size="<%= developers.length %>" multiple>
                <% developers.map((developer) => { %>
                    <option value="<%= developer.id %>"><%= developer.full_name %></option>
                <% }) %>
                <option value=""></option>
            </select>

            <button type="submit">Save</button>
        </form>
    </main>
    <%- include('../partials/footer.ejs') %>
</body>
</html>